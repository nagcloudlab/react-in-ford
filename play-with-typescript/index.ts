let myName: string = "Nag";

// myName = 123;

console.log(myName.toUpperCase());

interface IPerson {
  name: string;
  age: number;
}

let person: IPerson = {
  name: "Nag",
  age: 32,
};

console.log(person.age);
